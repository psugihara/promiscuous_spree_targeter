<%= form_for(@user_product_segment) do |f| %>
  <% if @user_product_segment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_product_segment.errors.count, "error") %> prohibited this user_product_segment from being saved:</h2>

      <ul>
      <% @user_product_segment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
